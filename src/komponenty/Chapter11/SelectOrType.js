import React from 'react';

const SelectOrType = () => {
    return (
        <form>
            <select>
                <option></option>
            </select>
        </form>
    );
};

export default SelectOrType;